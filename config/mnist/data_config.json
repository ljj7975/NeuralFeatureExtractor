{
    "data_loader": "MNISTDataLoader",
    "train_config": {
        "train": true,
        "data_dir": "data",
        "batch_size": 512
    },
    "test_config": {
        "train": false,
        "data_dir": "data",
        "batch_size": 512
    }
}